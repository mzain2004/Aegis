# Aegis Control Room

Frontend for the Aegis approval workflow. Run `npm install`, then `npm run dev` from this directory. In development, configure Vite to proxy `/api` to the FastAPI service or serve the frontend behind the same origin. The UI falls back to demo queue data when the API is unavailable.
